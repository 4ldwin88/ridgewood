# Setup / Gate 01 implementation checkpoint

This supersedes the initial contract-only scope in `v031-release-and-gate01-checkpoint.md`. Development continues in PR #18, independently of the v0.31 release branch and hosted backend. No production approver is presumed and no hosted migration has been applied.

## Implemented candidate

- Project Authorization & Setup drawer inside an opened Project State; twelve evidence sections with saved/reopened data, controlling references, accountable owners and material blockers.
- Explicit coverage for Project Lead, coordination/document control, commercial/finance, oversight and applicable field leadership. Roles may be combined; field non-applicability requires a reason.
- Append-only Setup versions retain the original frozen authorization reference. Saved readiness is derived in the database, including current membership of assigned people. Local edits are distinguished from the saved review basis.
- Explicit `project.setup.edit` permission. No new permissions are silently granted to workspace owners or role labels.
- Separate owner-identity/delegation records for Gate 01, requiring documented basis, dates, scope and revocation checks. No real authority rows are seeded.
- Immutable Go, Conditional Go, Hold and No-Go decisions, including authority/evidence snapshots and continuing obligations. Conditions require explicit category permission, reason, limits, owner, due date/trigger and consequence. Material and contractual blockers prevent advancement.
- Project row locking, version checks and request IDs protect concurrent saves and exactly-once advancement. Retrying a lost response reuses the original request and reads the committed record.
- Forward migration replaces the legacy transition with a governed-decision requirement and makes the old initializer read-only. Historical Gate 02 rows/decisions are retained; no project is automatically moved.

Migration filename `20260912001003_gate01_setup_contract.sql` was generated by Supabase CLI in disposable CI run 34660598201. The SQL was subsequently promoted from the development candidate and is replayed from scratch in CI. Do not edit already-applied production migration history when this is eventually released.

## Verification boundary

Initial persistence candidate `0c0e1dc` passed validation and database replay (34660293968 / 34660293975). Later candidate checks exposed a browser-test locator collision and a PL/pgSQL conditional-obligation alias collision; both were corrected. Those earlier passes are not proof for the final expanded candidate.

The current PR checks are authoritative for each exact head. Coverage includes unit readiness contracts, database persistence/access/decision tests, simultaneous save/decision attempts, and the real authenticated Opportunity → Authorization → Setup → Gate 01 → Mobilization workflow on a disposable backend. Browser tests capture desktop/mobile screenshots and intentionally lose save and decision responses.

## Release boundary and remaining review

Keep v0.31 live until the exact Setup candidate passes its CI and visual review, and an isolated interactive review is available. The local shell/repository and authenticated live browser work; local Supabase CLI startup failed in this environment. CI provides the disposable Supabase runtime. Do not use that limitation as a reason to migrate the shared hosted backend prematurely.

The owner authority binding is a production configuration task, not a development blocker. Company Decision Authority Matrix and Approval Matrix category W prohibit inferring this binding from tester membership or executive role. Continue implementation/testing with explicitly synthetic owner fixtures only.

Conditional obligations remain visible as frozen decision content after advancement. This stage does not yet implement Mobilization control workflows or an obligation-discharge command; do not erase or mark those conditions resolved by editing the original decision.
