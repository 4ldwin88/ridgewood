# v0.23 human-test failures and v0.24 repair

4ldwin’s 2026-09-10 human test failed. Automated checks had masked the stale Opportunity requirement because the rehearsal explicitly reloaded after saving an action. Hosted read-only inspection found zero inconsistent next-step requirement rows; the missing UI refresh was the reported blocker. The rehearsal now advances without that reload and checks both cancellation and restoration of the only qualifying action.

Other repairs: Attention uses a neutral derived “No attention needed” status; cross-stage tools remain open after successful commands; global save-completion auto-dismiss was removed; ordinary WorkspaceModal callers now render the shared native drawer while authorization confirmation remains compact; all six additional predevelopment forms report dirty/busy state; multiple work-state reporters are aggregated so nested evidence cannot clear a parent’s unsaved guard. Projects exposes archive and archived-history views using existing archive_project_state and authenticated reads. The archive regression verifies the same frozen authorization and seven publications remain after archiving. No hosted data reset, migration, or bulk archival is part of this work.

The displayed version becomes v0.24 because a real human test required this iteration. The full competitive shell redesign is not implemented by these fixes. Later lifecycle development and release changes remain separate. The prior v0.23 release stays preserved as historical evidence. v0.24 needs its own human retest after verified deployment.
